# /etc/nginx/sites-available/biopulse
# Nginx reverse proxy для Biopulse

server {
    listen 80;
    server_name biopulse.app www.biopulse.app;

    # Redirect to HTTPS (после настройки certbot)
    # return 301 https://$host$request_uri;

    # Ограничение размера загрузки (для PDF/фото)
    client_max_body_size 10M;

    # Next.js веб-дашборд
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Health check
    location /health {
        return 200 'ok';
        add_header Content-Type text/plain;
    }
}

# HTTPS (раскомментировать после certbot)
# server {
#     listen 443 ssl http2;
#     server_name biopulse.app www.biopulse.app;
#
#     ssl_certificate /etc/letsencrypt/live/biopulse.app/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/biopulse.app/privkey.pem;
#
#     client_max_body_size 10M;
#
#     location / {
#         proxy_pass http://127.0.0.1:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
